use anyhow::Result;
pub fn fetch_song(song_uuid: String) -> Result<Vec<u8>> {

    //some stuff to fetch data from s3
    //return fetched data
    //see if easy with rust, potentially copy from somewhere I've already implemented it
    // otherwise use nodejs, copy from digital ocean docs (they have it) and just do an API call here for it

    todo!()
}